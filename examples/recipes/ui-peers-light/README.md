# Recipe: UI peers (light)

Lightweight check to keep React/MUI/Emotion in `peerDependencies` (default WARN).

Run:
```
DEP_FENCE_CONFIG=examples/recipes/ui-peers-light/dep-fence.config.ts pnpm dep-fence
```
