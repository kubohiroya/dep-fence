# Recipe: UI peer policy

Package.json‑only check that keeps React/MUI/Emotion in `peerDependencies`.

Run:
```
DEP_FENCE_CONFIG=examples/recipes/ui-peer-policy/dep-fence.config.ts pnpm dep-fence
```

